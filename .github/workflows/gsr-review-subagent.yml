name: GSR Code Review (subagent)

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - uses: weitzer-org/gsr@feature/agent-selection-backend
        with:
          gemini-api-key: ${{ secrets.GEMINI_API_KEY }}
          mode: subagent
          agents: security
